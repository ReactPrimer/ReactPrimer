import React from 'react';
import ReactDOM from 'react-dom';
import { shallow } from 'enzyme';
import sinon from 'sinon';

import NewCompForm from '../src/NewCompForm';

describe('<NewCompForm />',() => {
xit();

})





/*
  check if entering component name triggers onChange
  check if clicking parent triggers onChange
  check if add component button triggers onClick
  check if  export button triggers onclick
*/
